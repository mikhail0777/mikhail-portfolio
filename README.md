# Mikhail Simanian — Portfolio (Vite + React + Tailwind)

Minimal, black, futuristic portfolio template.

## Run locally
```bash
npm install
npm run dev
```

## Customize
Edit `src/App.tsx`:
- projects list (title, stack, live demo, GitHub)
- hero text + quick links
- skills grid in `src/skills.tsx`

## Deploy
Works well on:
- Vercel
- Netlify
- Render (static site)

Build:
```bash
npm run build
```
